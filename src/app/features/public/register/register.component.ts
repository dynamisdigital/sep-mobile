import { Component } from '@angular/core';
import { IonContent } from '@ionic/angular/standalone';

@Component({
  selector: 'sep-register',
  standalone: true,
  imports: [IonContent],
  template: `<ion-content class="ion-padding"><p>Register</p></ion-content>`,
})
export class RegisterComponent {}
